import styled from 'styled-components'
import { cores, NavBar } from '../../styles'

export const NavBarRestaurante = styled(NavBar)`
  display: block;
  text-align: center;
`
export const HeaderText = styled.h2`
  font-weight: 900;
  font-size: 36px;
  line-height: 42px;
  color: ${cores.vermelho};
  padding-bottom: 40px;
`
